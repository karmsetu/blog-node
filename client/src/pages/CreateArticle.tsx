import FormField from '@/components/FormField';

const CreateArticle = () => {
    return (
        <div>
            <FormField method="POST" />
        </div>
    );
};

export default CreateArticle;
